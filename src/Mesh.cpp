#include "Mesh.hpp"

Mesh::Mesh(std::vector<char> voxels) {
	// TODO generate vertices from voxels
}

void	Mesh::render(glm::vec3 chunk_pos, glm::vec3 camera_pos, glm::vec3 camera_dir) {
	// TODO send vertices to opengl
}
